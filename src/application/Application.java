package application;

/**
 * TODO: написать аннотацию к классу
 */
public class Application {
    public static void main(String[] args) {
        /*Vector<Entity> table = new Vector<Entity>();
        table.add(new Entity(1, 84, 7));
        table.add(new Entity(8, 4, 7));
        table.add(new Entity(1, 5, 55));
        table.add(new Entity(41, 4, 9));
        Entity minstance = table.get(0);
        Field field[] = minstance.getClass().getDeclaredFields();
        for(int k = 0; k < field.length; k++) {
            String myfield = field[k].toString();
            System.out.println(myfield);
            System.out.println(myfield.substring(myfield.lastIndexOf(".") + 1, myfield.length()) + " | ");
        }
        /*System.out.println();
        for(int k = 0; k < field.length; k++) {
            System.out.print("----");
        }*/
        /*System.out.println();
        for(int i = 0; i < table.size(); i++) {
            Entity instance = table.get(i);
            System.out.println(instance.a.toString().length() + " | " + instance.b + " | " + instance.c);
        }*/
    }
}
